library(tidyjson)
library(magrittr)
library(stringr)
library(RSQLite)
library(tidyverse)
con = dbConnect(SQLite(),dbname = "../../data/participants.db");
con = dbConnect(SQLite(),dbname = "data/participants.db");
con = dbConnect(SQLite(),dbname = "data/participants.db");
con = dbConnect(SQLite(),dbname = "participants.db");
con = dbConnect(SQLite(),dbname = "/participants.db");
